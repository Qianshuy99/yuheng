# 玉衡主题开发规范

本文定义玉衡（YuHeng）主题的开发、集成、导出与验收标准。
玉衡由外壳与 Dubhe Core 负责主题管理；主题包只负责目标页面的视觉与必要交互。

## 1. 主题类型

| 类型 | 位置或载体 | JavaScript | 适用范围 |
| --- | --- | --- | --- |
| 内置主题 | `src/themes/<theme-name>/` | 允许 `mount()` | 需要创建 DOM、响应页面交互或实现复杂效果 |
| 导入主题 | JSON 文件 | 不允许 | 仅通过 CSS、变量与声明式设置项换肤 |

导入主题绝不能包含 `mount`、`unmount`、`script`、`js` 或 `code` 字段。Dubhe Core 不执行用户提供的代码。

## 2. 内置主题目录

新增内置主题使用以下结构：

```text
src/themes/<theme-name>/
  index.js       主题元数据与 mount/unmount 生命周期
  theme.css      主题 CSS
  assets.js      可选：内联资源，例如 data URI
```

- `<theme-name>` 使用小写字母、数字和短横线。
- 主题 ID 使用 `<family>.<name>` 形式，例如 `xp.luna`。
- CSS 通过 `import THEME_CSS from './theme.css'` 引入，不在 JavaScript 中拼接大段样式。
- 图片、字体等需要随主题分发的资源，应转成 data URI 或经构建脚本生成模块，不能依赖本机绝对路径。

## 3. 主题契约

每个主题必须导出一个主题对象，并至少提供 `id`、`name`、`version`、`match` 与 `css`。

```js
export const exampleTheme = {
  id: 'example.paper',
  name: 'Paper',
  version: '1.0.0',
  author: 'your-name',
  description: '简短说明主题效果与适用页面。',
  match: ['*://example.com/*'],
  runAt: 'start',
  css: THEME_CSS,
  vars: {
    '--example-accent': '#2f6f61',
  },
  settings: [
    { key: 'compact', type: 'bool', label: '紧凑布局', default: false },
  ],
  source: 'builtin',
  mount(ctx) {
    // 可选：创建该主题所需的 DOM 和事件监听器。
    return () => {
      // 必须清理 mount 创建的 DOM、监听器、计时器和行内样式。
    };
  },
};
```

字段要求：

| 字段 | 要求 |
| --- | --- |
| `id` | 必填；1-64 个字符，只能使用字母、数字、`.`、`_`、`-`；全局唯一。 |
| `name` | 必填；面板中展示的主题名。 |
| `version` | 必填；建议使用语义化版本，例如 `1.0.0`。 |
| `author` | 建议填写；用于主题归属。 |
| `description` | 建议填写；不超过 200 字符。 |
| `match` | 必填；至少一个 userscript 风格的匹配模式，例如 `*://*.example.com/*`。 |
| `runAt` | `start` 或 `idle`；首屏样式选择 `start`，非关键增强可选择 `idle`。 |
| `css` | 必填；主题的完整 CSS 字符串。 |
| `vars` | 可选；键必须是 `--` 开头的合法 CSS 自定义属性。 |
| `settings` | 可选；仅支持 `bool`、`select`、`text`。 |
| `mount` | 仅内置主题可用；返回卸载函数。 |
| `onSetting` | 仅内置主题可用；就地处理设置后返回 `true`，否则引擎会重新挂载主题。 |

`match` 采用 `scheme://host/path` 三段格式。支持 `*` 协议、`*` 主机、`*.example.com` 子域名与路径通配符；不依赖未声明的匹配行为。

## 4. CSS 编写标准

### 4.1 作用域

主题 CSS 必须以主题根类作为作用域。引擎会将主题 ID 中的 `.` 替换为 `-`，并写入 `html`：

```css
html.yh-theme-example-paper .App {
  --example-panel-bg: #fffdf7;
}

html.yh-theme-example-paper .Button {
  border-radius: 2px;
}
```

禁止使用无作用域的 `:root`、`html`、`body` 或裸页面选择器。全局变量应通过 `vars` 提供；主题卸载后，注入的 `<style>` 会整体移除。

### 4.2 选择器与兼容性

- 优先使用页面稳定的语义类、`data-*` 属性和组件根节点，不以 DOM 层级或临时文本作为主要定位方式。
- 避免 `!important`；仅在覆盖第三方行内样式或无法控制的高优先级规则时使用，并限定到主题作用域。
- 不得修改 `#yh-shell` 及其子节点。玉衡外壳使用独立的青瓷绿视觉语言，不属于主题的覆盖范围。
- 所有新增的固定定位层都必须评估与玉衡悬浮球、面板、站点弹窗的层级冲突。
- 主题在窄屏下应保留可用的导航、主内容与操作入口；不以隐藏关键功能来换取视觉一致性。

### 4.3 资源与网络

- 主题内资源优先使用 data URI；导出的 CSS-only 主题不能依赖 `@resource` 或本地文件。
- 禁止 `@import`、`expression()`、`-moz-binding`、`behavior:url()`、`javascript:` 与 `data:text/html`。
- 若使用 `https://` 或协议相对 URL 的外链资源，导入确认页会列出目标域名。仅在资源确有必要且来源可信时使用。
- 单个主题 CSS 不得超过 512 KB；`vars` 合计不得超过 512 KB。

### 4.4 声明式布局

主题包可选地声明 `layout`，由引擎把宿主页面已有的直接子节点标记为稳定区域，并在主题生效期间应用 CSS Grid。默认布局不移动 Flarum 或其他 SPA 管理的节点，因此路由、事件绑定和卸载都保持可恢复。

```json
{
  "layout": {
    "root": "#app",
    "regions": [
      { "id": "header", "selector": ":scope > #drawer" },
      { "id": "content", "selector": ":scope > .App-content" }
    ],
    "desktop": {
      "columns": "220px minmax(0, 1fr)",
      "rows": "auto minmax(0, 1fr)",
      "areas": ["header header", "content content"]
    },
    "mobile": {
      "maxWidth": 760,
      "columns": "minmax(0, 1fr)",
      "rows": "auto minmax(0, 1fr)",
      "areas": ["header", "content"]
    }
  }
}
```

- `root` 是布局根节点选择器；每个 `regions[].selector` 必须命中其直接子节点。
- `id` 只能使用字母、数字、`_` 和 `-`，并且要出现在 `areas` 中。
- 运行时会写入 `data-yh-layout-root` 和 `data-yh-layout-region`。主题 CSS 可以使用这些属性完善区域内部样式，但不应依赖未声明的 DOM 层级。
- `desktop` 必填；`mobile` 可选，`maxWidth` 默认 760。网格值只接受安全的 CSS Grid 轨道表达式，不能携带规则块或任意 CSS。
- 区域高度由 `desktop.rows` 与 `mobile.rows` 声明，例如 `"56px minmax(0, 1fr)"` 表示头部固定 56px、内容占据剩余高度；也可以使用 `minmax()`、`fr`、`auto` 等标准 Grid 轨道值。此能力是主题布局声明的一部分，不会在用户面板额外生成高度调节控件。
- 卸载时布局样式和所有运行时属性都会还原。若页面没有准备好，引擎会等待所声明区域出现；不要用布局字段接管弹窗、悬浮菜单或 SPA 动态插槽。

### 4.5 保留 SPA 路由会替换的导航

当邮件式布局需要在帖子页继续保留列表页左栏时，可声明 `layout.portals`。引擎会在指定最小宽度以上，把命中的**现有节点本身**转移到主题门户；链接、按钮和站点原有事件绑定不会被复制或重建。返回来源页面时，门户会接管新创建的同一导航节点；主题卸载或窗口变窄时会还原。

```json
{
  "layout": {
    "portals": [
      {
        "id": "primary-nav",
        "source": "#content .IndexPage-nav.sideNav",
        "minWidth": 1200
      }
    ]
  }
}
```

- `source` 必须是有效 CSS 选择器；每个主题最多 4 个门户。
- `minWidth` 默认 992，推荐设置为主题三栏布局的折叠断点。
- 门户仅适合导航、筛选等可独立保留的区域；不要迁移编辑器、弹窗、通知或帖子正文。

## 5. 生命周期标准

内置主题的 `mount(ctx)` 可能在切换主题、关闭主题或修改设置时多次运行，必须满足可重挂与可卸载。

1. 创建的 DOM 节点使用主题私有前缀，例如 `example-paper-toolbar`。
2. 记录并在卸载函数中移除所有事件监听器、`setTimeout`、`setInterval`、`MutationObserver` 与动态节点。
3. 若修改 `document.documentElement` 的 class、CSS 变量、`body` 行内样式或属性，必须在卸载时恢复原值。
4. 不保存跨页面的临时节点引用；页面跳转后由新一次挂载建立状态。
5. `mount` 应允许在页面 DOM 尚未完整时运行。需要页面元素时，使用等待 DOM 的受控机制，并确保其可取消。

可用上下文：`ctx.settings`、`ctx.setSetting(key, value)`、`ctx.toast`、`ctx.alert`、`ctx.confirm`、`ctx.dialog`、`ctx.openPanel`、`ctx.openAbout`、`ctx.disableTheme()` 与 `ctx.log`。

## 6. 设置项标准

设置项的 `key` 只能使用字母、数字与下划线，长度不超过 40。设置应是主题私有的、可理解的用户偏好，不应用于保存临时运行状态。

```js
settings: [
  { key: 'enabled', type: 'bool', label: '启用动态背景', default: true },
  {
    key: 'density',
    type: 'select',
    label: '信息密度',
    default: 'comfortable',
    options: [
      { value: 'compact', label: '紧凑' },
      { value: 'comfortable', label: '舒适' },
    ],
  },
]
```

- `bool` 的默认值为布尔值。
- `select` 必须提供至少一个 `options` 项，每项包含 `value` 与 `label`。
- `text` 用于短文本偏好，不承载 CSS、HTML 或脚本片段。
- 设置改变能原地更新时，实现 `onSetting(key, value)` 并返回 `true`；否则由引擎卸载并重新挂载。

## 7. 导入主题格式

导入主题是主题对象的 JSON 序列化版本。它只能携带数据字段，不含函数与运行时代码：

```json
{
  "id": "example.paper",
  "name": "Paper",
  "version": "1.0.0",
  "author": "your-name",
  "description": "用于 example.com 的纸张风格主题。",
  "match": ["*://example.com/*"],
  "runAt": "start",
  "css": "html.yh-theme-example-paper .App { color: #222; }",
  "vars": {
    "--example-accent": "#2f6f61"
  },
  "settings": [
    { "key": "compact", "type": "bool", "label": "紧凑布局", "default": false }
  ]
}
```

导入前应使用主题面板完成验证。内置主题的 `mount`、`onSetting` 等能力不属于可分发 JSON 格式。

## 8. 开发与验收

在 `yuheng/` 目录执行：

```bash
npm install
npm run preview
npm run check
npm run shots
npm run build
```

提交内置主题前，至少完成以下验收：

- 在每条 `match` 对应的目标页面验证挂载成功；非匹配页面不应注入主题样式或 DOM。
- 验证首次挂载、切换到其他主题、选择“不使用主题”、刷新页面和修改每项设置。
- 关闭主题后确认主题 `<style>`、根类、动态节点、事件监听器和行内覆盖均已清理。
- 在桌面与窄屏宽度检查文本、导航、表单、弹窗、悬浮控件与滚动行为。
- 运行 `npm run check`；涉及视觉结构时运行 `npm run shots` 并检查截图。
- 运行 `npm run build`，确认 `dist/yuheng.user.js` 与源码同步。

## 9. 发布要求

内置主题的修改属于发布产物的一部分。完成开发后必须执行 `npm run build`，并将更新后的 `dist/yuheng.user.js` 一同提交；不要直接编辑 `dist/` 内文件。

主题名称、作者、版本、适用站点和主要设置项变更时，同时更新主题说明与相关截图，保证面板展示、导出内容和文档描述一致。
