已读完品牌方案、现有 aicue-xp-skin.user.js（1324 行，CSS 常量 + DOM 构建全在一个 IIFE 里）和两个素材。先说三个必须在动手前纠正的前提，再给规划。

需要先纠正的前提
油猴脚本没有 popup 页。 方案第二章把「核心阵地」定为 popup.html + popup.css，那是浏览器扩展的形态。Tampermonkey 只给你 GM_registerMenuCommand（一行行文字菜单，不能塞 HTML）。所以品牌主阵地必须是第四章的悬浮控制器，popup 那套布局/CSS 直接改造成面板的样式即可，不浪费。

素材与色板冲突，按你的选择以 logo 为准。 logo.png 实测是 #A8CCC0 一带的青瓷绿（363×512，纵向，不透明像素仅 26%），文档的 #4A90E2 玉衡蓝配不上它。新色板：


--yh-jade:      #A8CCC0   主色（悬浮球、按钮、分区标题）
--yh-jade-deep: #7FA79B   悬停/边框
--yh-bg-dark:   #16211F   面板底（偏冷灰绿，替代 #1A1A2E）
--yh-accent:    #E8D9A0   点缀（替代星辰金 #F8E71C）
--yh-text:      #E4EAE7 / --yh-text-muted: #8A9793
XP 主题内部继续用 Luna 蓝（#0054e3 / #ece9d8）不动——外壳是「管理器」，主题是「被管理的内容」，两套色分工清楚。

文档内几处硬错误，改品牌文案时一起修：控制台 banner 写「北斗第七星」，与第一章「玉衡——北斗第五星」矛盾（第五星才对，第七星是摇光）；@namespace https://yuhneg.skin 是 yuheng 的拼写错误且域名不存在，应指向仓库地址；产品 v1.0 却配「Dubhe Core v2.1」，建议单一版本号，底部写 YuHeng v1.0.0 · Dubhe Core。

品牌名保留「玉衡」，logo 后续重做——brand.js 里留 LOGO_IS_PLACEHOLDER 标记，现有 logo 补白成方图先用。

架构：引擎 / 主题 两层分离
单个 userscript，@match *://*/*，@run-at document-start。Tampermonkey 的 GM_getValue 是同步的，所以能在 document-start 读配置后立即早退——没有注册主题的站点，引擎除了一次 GM_getValue 之外零开销，这是 *://*/* 能被接受的前提。


yuheng/
  src/
    brand.js            品牌常量（名称/版本/色板/logo base64）唯一来源
    core/
      store.js          GM_getValue/setValue + localStorage 兜底（沿用现有 try/catch 风格）
      log.js            YHLog（info/ok/warn/error/dubhe），banner 只打一次
      registry.js       主题注册、host 匹配、当前激活主题解析
      injector.js       <style> 注入/卸载、CSS 变量覆写、切换过渡
      validate.js       导入包的校验与清洗
      ui/               ball.js panel.js toast.js loading.js dialog.js empty.js
    themes/xp/
      index.js          meta + mount()/unmount()（DOM 部分）
      theme.css         从现有脚本剥离的 CSS 常量
      wallpaper.js      背景图 data URI
    main.js
  assets/               logo.png background.jpg（源文件）
  scripts/build.mjs     esbuild 打包成 dist/yuheng.user.js（照抄隔壁 awesome-aicue-reader 的做法）
  scripts/gen-assets.py 生成方形图标 + 裁剪后的壁纸 base64
  preview/              沿用现有离线样板 + GM_* mock
主题契约（CSS-only 包就是这个对象的 JSON 序列化，内置主题多了 mount/unmount）：


{
  id: 'xp.luna',            // 反查唯一
  name: 'Windows XP',
  version: '1.0.0',
  author: 'xiyan',
  match: ['*://*.aicue.top/*'],   // 引擎按此决定是否激活
  runAt: 'start',                  // start | idle
  css: '...',                      // 唯一必需的样式载荷
  vars: { '--primary-color': '#0a246a' },
  settings: [{ key: 'boot', type: 'bool', label: '开机动画', default: true }],
}
存储：yh:config（{enabled, ball, activeByHost: {host: themeId}}）、yh:themes（导入包数组）。分两个键，主题包大而配置改得频繁，别互相拖累。

导入的安全边界（v1 只收 CSS）
引擎不 eval 任何用户内容，css 字段进 injector 前过 validate.js：拒绝 @import（可绕过体积与来源限制）、javascript:/data:text/html URL、expression(、-moz-binding；单包上限 512 KB；url() 指向站外域名时不阻止但在导入对话框里列出来提示「该主题会向 X 发起请求，可被用于记录你的访问」——CSS 拿不到页面数据，但外链请求确实是一个可观测信道，值得让用户知道。带 JS 的主题包不做，等于给插件开任意代码执行入口。

壁纸处理
background.jpg 是 1523×952，右下角有微软品牌：白字 "Microsoft WINDOWS" 占 x 813–1443 / y 746–842，四色旗占 x 1247–1486 / y 676–827；草地地平线在 y≈745–780（约 78%）。单纯裁剪去不掉——要么切成竖图，要么把草地切没了。方案是取左侧干净区（x 0–760，含天空和草坡）水平镜像补满右半，得到 1520×952 无品牌图；草和云是软质纹理，接缝不可见。再缩到 1280×800（保持 1.6 比例、地平线位置不变）、JPEG q72，约 55 KB → base64 约 75 KB，内联进 wallpaper.js。因为 v1 主题包是纯 CSS，壁纸必须以 data URI 随包走，不能靠 @resource——那样导出的主题在别人机器上就是空背景。

图标同理：logo 是 363×512 的竖构图，直接缩成 16×16 会糊成一团。gen-assets.py 用 Pillow 按最长边等比缩放后补透明边成正方，产出 16/32/64/128 四档（对应油猴图标、面板头部、关于弹窗、空状态水印）。Pillow 在这台机器上可用，已验证。

里程碑
M0 品牌收敛——brand.js 单一来源、新色板落地、修上面三处文档错误、gen-assets.py 产出图标与壁纸 base64。

M1 Dubhe Core——store / log / registry / injector / validate。可验证点：注册两个假主题，按 host 正确激活，切换与关闭无残留 <style>，无主题站点 document-start 早退。

M2 外壳 UI——悬浮球（拖拽需修方案里的 bug：mousedown 后 click 必然触发，拖完就会误开面板，得按位移阈值区分）、面板、Toast、Loading、空状态、关于对话框、GM 菜单（🌟 玉衡：… 前缀）。

M3 XP 主题包化——把现有 1324 行拆成 theme.css + index.js 的 mount/unmount，壁纸 Bliss 换成真图（Luna/纯色两档纯 CSS 保留），旧 xpw:* localStorage 一次性迁移到新 schema，Ctrl+Alt+X 保留。这一步的风险是现有脚本的 chrome 是 position:fixed + 改 --header-height 换来的「不接管滚动」，unmount 必须把这些变量和 body padding 一并还原，否则关主题后 Flarum 侧栏 sticky 位置会错。

M4 导入导出 + 回归——导入对话框（粘贴 JSON / 拖文件）、导出当前主题、preview/ 加 GM mock 页，沿用你已有的 hash 钩子截图法做视觉回归。

确认这个方向，我就从 M0 开始：先建目录骨架、brand.js 和资产脚本，跑出图标和壁纸给你看效果。

20260729
皮肤做完了，收尾的这几项也一并处理了。

新增/修好的

讨论页现在也过了一遍：.DiscussionPage-nav（Scrubber + 回复/关注按钮）做成了和首页左栏同一套的 XP 任务窗格，Scrubber 轨道用点阵底纹，.DiscussionHero 变成米色标题条；回复框 .Composer-header 画成了蓝色渐变标题栏，整个 Composer 就是一个从底部升起的 XP 窗口。
站点自带的右下角悬浮「切换站点」胶囊（#site-switcher-container，closed shadow root + z-index:99999）会正好压在任务栏上。它挂在 closed shadow 里，外部 CSS 进不去，也不能靠 transform 造包含块（那会把里面 inset:0 的弹窗压成 0×0），所以整个隐藏，改在「查看 → 切换站点」和开始菜单里补了等价入口——自绘 XP 对话框，www.aicue.top ↔ flarum.aicue.top，路径带过去。关于框里也写明了这一点。
清掉了工作目录里的临时物：13 个一次性 Chrome profile、下载来的 aicue-home.html、以及一张没被引用的 assets/background.jpg（壁纸全是纯 CSS 画的，用不上）。forum.css 移进了 preview/，它只是预览样板的参照，不该待在发布目录。
验证
node --check 通过。headless Chrome 1280 宽新截了首页和讨论页，加上之前的开始菜单、最小化桌面、开机动画、关于框、切换站点框，共八个状态都在 preview/ 下的 shot-*.png。首页右下角确认已经没有那颗蓝胶囊了（预览里我用同样的 closed shadow root 复刻了一个来测）。

产物

aicue-xp-skin.user.js — 唯一需要发布的文件
README.md — 安装、功能、实现要点（为什么不接管滚动）、预览用法
preview/ — 离线样板，不参与发布
仍然只在本地样板上验证过视觉，没在线上 aicue.top 实测过；真机上如果 Flarum 装了额外扩展带来新 DOM，可能还要补几条选择器。